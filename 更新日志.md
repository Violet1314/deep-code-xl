# 更新日志

---

## v0.40.0（2026-08-12）

- **CTRL+C 立即打断 AI 流式生成** — 修复 raw 终端模式下 CTRL+C 失效问题（`MakeRaw` 后 Windows 不再产生 `CTRL_C_EVENT`，轮询器只识别 ESC 导致 0x03 被忽略）；中断轮询改用共享纯函数同时识别 ESC 与 CTRL+C，权限确认也支持 CTRL+C 取消，状态栏文案更新为 `Esc/Ctrl+C interrupt`
- **菜单样式：仅选中行高亮（DeepSeek 蓝）** — `/` 命令补全/参数补全菜单重构，未选中项与帮助行恢复无背景纯文本，仅选中行使用 DeepSeek 深蓝 `#3A56D4` 背景 + 白字
- **移除 AI 摘要压缩，仅保留结构化归档压缩** — 删除 AI 摘要胶囊压缩器，`TryCompressForRequestSize` 重构为纯归档调度，压缩收敛为本地无损归档单层策略，零 API 成本；session 快照移除压缩块持久化，`/context cache` 诊断去掉 `ai=` 列
- **归档压缩体验优化（P0+P1）** — ① 占位符新增前 400 字符 `Preview:` 内容头，模型对归档内容有方向感、90% 场景免去恢复往返；② 按内容 sha256 哈希去重，同一文件反复读取不再重复归档，档位利用率翻倍；③ 保护轮 1→2，避免"刚读大文件→下轮就要用→只剩占位符"的体感断裂
- **归档触发次数调优 + 展示去 AI 化** — 档位上限 3/9/18/30 → **5/8/10/12** 适配真实需求；`/context compression` 面板删除 AI 时代遗留 `AI Per Tier` 行，文案改 `no API calls`/`lossless-local`
- **system 段稳定化（prompt cache 命中率优化）** — 诊断命中率仅 8.3% 根因：动态内容拼进 system 前缀导致缓存前缀漂移、全量 miss；重构后 `buildSystemPrompt` 只返回静态部分，动态上下文（Skill 选择/Recent Read Context/Current Task State/Edit Activity）合并为一条 `# Live Context` user 消息在 payload 末尾注入，不写入会话存储；system 前缀逐字节稳定，预期命中率 8.3%→50%+
- **静态并行工具调用引导（轮次优化）** — 实测 `tool_calls=224 tool_rounds=147`（平均 1.52 调用/块）确认模型几乎不并行；并行工具调用引导改为常驻静态提示拼入默认系统提示，保证每次请求可见且不破坏 system 前缀稳定性
- **验证** — `go build ./...` 通过、`go test ./...` 全 14 包 0 失败、Linux 交叉编译通过（验证 `interrupt_other.go`），确认 0 回归

---

## v0.39.1（2026-07-11）

- **自更新多镜像加速与分层超时** — `update/update.go` 下载链路重构：新增 `defaultMirrors`（gh-proxy.com + GitHub 直连）前缀拼接镜像列表，按序尝试，GitHub 直连作为最终 fallback；`DEEPCODE_MIRROR` 环境变量支持 `off`（仅直连）/ `URL`（自定义镜像 + fallback，自动补全末尾 `/`）/ unset（默认镜像列表）；`buildDownloadURLs` 按环境变量生成候选 URL 列表，`mirrorDisplayName` 在进度条展示当前镜像友好名；分层超时替代原固定 5min：TCP/TLS/header 各 15s（`dialTimeout`）+ stall 检测 60s（`stallTimeout`，`stallReader` 每 5s 检查 lastActivity，无数据即 `cancel()`）+ 总计 10min 硬顶（`maxDownloadTimeout`，所有镜像共享）；全部镜像失败时打印手动下载 URL 提示；新增 `TestBuildDownloadURLs`/`TestMirrorDisplayName`/`TestStallReader_*` 测试覆盖
- **npm install.js 多镜像 fallback** — `npm/install.js` 下载链路同步重构：新增镜像列表（gh-proxy.com + GitHub 直连）与 `buildDownloadURLs` 函数，支持 `DEEPCODE_MIRROR` 环境变量（自动补全末尾 `/`）；`tryDownload(index)` 递归 fallback 逐个尝试；新增 stall 检测（`stallTimer` 每 5s 检查，20s 无数据即 abort 切换镜像）+ 单镜像总超时 3min（`TOTAL_TIMEOUT`）+ socket 空闲超时 20s（`SOCKET_TIMEOUT`，兼作连接超时）；镜像切换时打印提示信息
- **移除不可用镜像** — 实测 `ghproxy.com`（连接超时）与 `ghps.cc`（SSL 握手失败）已不可用，从 `update.go` 与 `install.js` 镜像列表中移除，避免每次更新白白浪费 ~30s 连接超时
- **验证** — `go test ./update/ -v -count=1` 8 项全通过（含新增 `env_custom_mirror_no_trailing_slash` 子测试），`go build ./...` 与 `node -c install.js` 编译通过，确认 0 回归

---

## v0.39.0（2026-07-03）

- **File Cache payload 降载** — 完整 Read 结果新增 `ViewMode`、`Summary`、hash 与行数元信息，历史未变文件在 payload 中自动替换为 cached placeholder，保留恢复提示并减少重复正文进入请求体
- **Read Range Memory 与恢复提示优化** — cached placeholder 接入已读区间记忆，优先选择更宽、更靠后的已读范围生成 `Restore: Read file_path=... offset=... limit=...`，降低长会话重复整文件读取概率
- **Edit 参数预检查** — Edit 工具抽出 `parseEditMode`，在读写文件前校验精确匹配模式与行号模式；`start_line/end_line` 缺一时直接返回明确错误，减少无效执行轮次
- **固定层级上下文压缩** — request-size 压缩从旧 backoff/preset 模型收敛为固定层级与次数限制，`/context compression` 仅保留 `on/off/reset`，诊断展示真实 AI/Archive 分档状态与 900K 上限
- **Large Content Archive 双通道计数** — AI 摘要压缩与本地无损 Archive 分离计数，Archive 优先且不消耗 AI 次数；archive 占位卡片明确标识为本地无损索引，并通过 `ContextArchiveRead` 恢复原文
- **轻量执行状态注入** — 新增 `Current Task State`，记录当前目标、已读取文件、已修改文件与验证命令；状态随 `/save`、`/history`、`/resume` 保存恢复，用于减少重复探索和漏跑验证
- **最近读取上下文卡片** — 新增 `Recent Read Context`，记录最近 Read 的 path/hash/total_lines/view/read_ranges/summary/restore_hint，提示模型优先复用已读摘要和恢复提示
- **同文件编辑活动提示** — 新增 `Edit Activity`，记录当前任务中同文件 Edit/Write 次数，提示模型在已编辑文件上执行 consolidation pass，合并剩余修改和验证修复，避免反复小补丁
- **验证失败错误集合** — Bash 在 `go test` / `go build` 失败时保留更多输出并追加 `Verification error set`，提取关键错误线索，引导一次性修复全部错误后再重跑
- **模型侧提示词英文化** — 内置 agents、skills、工具 schema 与压缩器 prompt 统一为英文，保持模型侧输入一致；用户侧中文 CLI 文案不做大范围改动
- **RepoMap 任务地图升级** — `/repomap general` 从目录地图升级为 `Task Workspace Map`，接入本轮输入、当前任务状态、Recent Read、Edit Activity 与 Go 符号命中，生成 `Likely relevant files`；保留当前目录地图作为子集，并增加目录职责推断与实现/测试配对
- **Developer RepoMap 调试模式** — `/repomap dev` 全量图标题调整为 `Developer RepoMap`，明确作者调试定位，并增加 10 秒构建超时保护
- **验证** — `go test ./... -count=1` 与 `go build ./...` 全量通过，确认 0 回归

---

## v0.38.0（2026-06-28）

- **Edit `no_match` 智能引导** — `old_string` >15 行或 >500 字符时，`no_match` 错误自动建议改用 `start_line`+`end_line` 行号模式，避免 AI 陷入"重读→重试→失败"死循环
- **内置 Skill 规则体系** — `tool-planning` 新增 Edit 工具规则（大段用行号、no_match 后禁止重读、信任 content_hash）与 Read 反模式清单；`cost-control` 新增 Cost Quantification 量化表格，统一指向 Tool Planning Protocol 消除跨文件重复
- **Read 小窗口提示升级** — 碎片化读取判定从 `<15%` 提高到 `<25%`，警告从温和 `Hint:` 升级为 `⚠️ FRAGMENTED READ`，新增成本警告和推荐窗口
- **RepoMap 双模式** — 新增 `/repomap` 命令（别名 `/rmap`），通用模式（默认）轻量级 Workspace Context 自动检测项目根与语言提示、随 `/cd` 动态重建；开发者模式全量 Go 符号地图持久缓存。5 维硬上限（文件/目录/超时）防御任意目录卡死。接入内联菜单补全
- **RepoMap dev 密码保护** — `/repomap dev` 需输入密码（无回显），最多 3 次提示 + 第 4 次错误直接退出防暴力破解；验证通过后 `config.json` 持久化，重启自动免密码；`/help` Workspace 区补充条目
- **上下文压缩按需激活** — `context-compression` skill 从 `always: true` 改为按需触发（archived/compressed/placeholder 等关键词），简单查询每轮节省约 15 行系统提示
- **编译检查前置** — Go File Write/Edit Pre-Check 规则：写文件前确认模块名/import/函数可见性、同轮编译、一次修完所有错误
- **验证** — `go test ./...` 通过，529 测试 0 回归

---

## v0.37.0（2026-06-26）

- **上下文压缩系统重构** — 新增 Large Content Archive，大型历史工具输出优先写入本地归档，payload 中替换为占位符，降低上下文占用且保留原文可恢复
- **ContextArchiveRead 工具** — 新增只读工具，可按 `arch-xxxxxx` 恢复历史归档原文，用于压缩后仍需精确代码、错误日志或工具输出的场景
- **AI 摘要胶囊** — archive 无候选时 fallback 到结构化短摘要，保留任务摘要、关键决策、文件路径、archive refs 与下一步动作，避免旧版长摘要超时和高成本
- **压缩档位控制** — `/context compression` 支持 `off/stress/low/medium/high/max/reset`，默认 `low=200K→120K`，压测可用 `stress=50K→30K`
- **Compression Backoff 防抖** — 每次有效压缩检查后 next trigger 按 50K 推进，避免低阈值下连续重复压缩，`/context cache` 可查看 steps 与 next trigger
- **上下文恢复一致性** — session snapshot 持久化 compressed blocks/watermark，`/history`、`/resume` 恢复后继续使用压缩胶囊与 archive refs，`/new` 和 clear 会同步清理相关状态
- **读取效率优化** — 最近完整 tool result 保留提升到 8 条，按工具类型设置历史压缩上限，新增 Read Range Memory 记录已读区间，减少大文件重复读取
- **工具执行优化** — 只读并行工具去重，同批重复 Read/Grep/Glob/ContextArchiveRead 只执行一次；Grep 支持 path 直接传入单个文件
- **诊断信息增强** — `/context cache` 增加 Archive、File Cache、Tool Efficiency、Compression Backoff、Last Compression 等字段，便于观察上下文占用、压缩效果和重复读取
- **AI capsule 精确细节保留** — 压缩 prompt 强化数字阈值、状态流转、函数名、文件路径、用户约束和 `/context cache` 运行态事实，降低压缩后细节混淆
- **稳定性修复** — 修复 Last Compression 空 attempt 覆盖、`/new` 继承旧压缩状态、压缩 success 后连续触发等问题
- **验证** — `go test ./... -count=1` 与 `go build ./...` 已通过，当前 529 个测试用例，0 回归

---

## v0.36.0（2026-06-21）

- **Provider 预设大幅精简** — 从 8 个砍至 2 个：仅保留 DeepSeek 官方 + openai-anthropic 双协议；模型改为 API 全量发现，不再硬编码
- **/connect login 交互式改造** — ↑↓ 方向键选择替代数字输入，API Key 脱敏显示，保存后自动连通性检查
- **双协议 Anthropic 支持** — OpenAI 协议失败自动 fallback 到 Anthropic，完整实现 Anthropic SSE 解析
- **AI 驱动上下文压缩** — 对话占比达 60% 自动触发，按轮次整批压缩，最近 5 轮保护
- **工具效率优化 6 项** — 骨架视图、文件缓存、Patch Preview、Repo Map、并行执行、读取约束
- **ESC 打断输出** — 流式输出中按 ESC 立即终止，已生成内容保留到对话历史
- **对话限制全面提升** — 单次最大轮次 60→100，连续工具收敛 40→60，锚定消息 2→4
- **终端 UI 重构** — 冻结头部移除、StatusLine 单次渲染、头部精简、间距修复、max_tokens 去硬编码
- **渲染细节** — 背景块统一 98% 宽度对齐、CJK/emoji 安全截断、/model list 50 上限

---

## v0.34.0（2026-06-18）

- **自动更新引擎** — 每次启动检查 GitHub Releases，发现新版自动下载替换
- **npm 一键安装** — `npm install -g deep-code-xl`，无需手动下载
- **首次运行引导** — 自动弹出交互式配置界面
- **mono 主题** — 代码块黑白一致性修复，持续完善
- **恢复链路** — 规范化 DSML/XML 防线，session replay events 重构
- **Patch Preview** — Write/Edit 恢复回放渲染，矩形背景与命令块顶部间距统一
- **default 主题** — 第一轮打磨
